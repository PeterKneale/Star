- Operations
  - SetTags
    - Request - `{ Id, Tags[] }`
    - Reply - `{ }`
  - GetTags
    - Request - `{ Id }`
    - Reply - `{ Tags }` }`
    
- Events
 - TagsSetEvent - `{ Id, Tags[] }`
